# %%
import os
import pandas as pd

# Diretoria onde está o app.py
DIR_PATH = os.path.dirname(__file__)

# Caminho para a pasta "csv" que está ao lado de app.py
csv = os.path.join(DIR_PATH, "csv")
CSV_PATH = os.path.join(csv, "ALL.csv")

